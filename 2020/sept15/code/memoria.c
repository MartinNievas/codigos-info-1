#include <stdio.h>

int main(void)
{

  int entero;
  double decimal;

  printf("Letra: %ld\n", sizeof(char));
  printf("Entero: %ld\n", sizeof(entero));
  printf("Decimal: %ld\n", sizeof decimal);

  return 0;
}
