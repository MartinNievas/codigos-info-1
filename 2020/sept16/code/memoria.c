#include <stdio.h>

int main(void)
{

  float decimal; // IEEE754 (32bits) 1-8-

  // bytes <- sizeof (arg)
  // ++entero;
  printf("Entero: %ld\n", sizeof(int));
  printf("Decimal: %ld\n", sizeof decimal);

  return 0;
}
