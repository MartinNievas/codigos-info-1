#include <stdio.h>
#define TAM 100

void ordenar_arreglo(int* arreglo, int tam, int sentido);

int main(void) {

  int arreglo[TAM];

  // De mayor a menor
  ordenar_arreglo(arreglo, TAM, 0);
  // De menor a mayor
  ordenar_arreglo(arreglo, TAM, 1);

  return 0;
}

void intercambia_si_menor(int* arreglo, int j){
  if (*(arreglo+j) > *(arregloj+1)){
  }
}

