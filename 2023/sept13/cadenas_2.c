#include <stdio.h>

int main(void) {

  char palabras[10][5];
  // Cargar 10 palabras
  scanf("%s", palabras[0]);

  // Imprimir las palabras cargadas
  printf("%s\n", palabras[0]);
  printf("%s\n", palabras[1]);

  return 0;
}
