#include <stdio.h>

/* La función recibe un entero
 * y devuelve:
 * 1: si el entero es impar
 * 0: si el entero es par
 * */
int es_impar(int);


int main(void) {
  float a = 10.1, b = 20.2;

  printf("%d\n", sumar(a,b));

  return 0;
}
