#include <stdio.h>

int main(void) {

  // Escribir un programa que solicite al usuario
  //
  // Ingresar dos números reales, guardarlos en variables
  // diferentes
  //
  // Imprimir la suma, resta, producto, cociente y resto
  // entre los números. Mostrar por pantalla

  return 0;
}
