#include <stdio.h>
#define TAM 6

int buscar(int arr[], int tam, int clave){

}

int main(void) {

  int arreglo[TAM] = {1,2,3,5,54,23};
  int num = 4;
  int pos = buscarelem(arreglo, TAM, num);

  printf("El elemento %d, está en la posición:%d\n", num, res);

  return 0;
}
