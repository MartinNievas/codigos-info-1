#include <stdio.h>
// Notas de clase
// No compila


int main(void) {

  //rb+
  fopen("algo.txt",...);

  if (fptr == NULL){
    ;
  } else {

    while (!feof(fptr)){
      fscanf(fptr, "%s");
    }
    fprintf(fptr,"Algo al final");

    fclose(fptr);
  }

  return 0;
}
