// Se desea invocar una función como argumento de la función printf; dicha función recibe
// parámetros por valor: una estructura de 3 miembros de tipo float y devuelve un puntero a un
// tipo de dato entero. ¿Cuál es la opción correcta?
// Seleccione una:

a. printf (“ %d”, TTT(struct dato t ));
b. printf (“ %d”, * (TTT(a,b,c )));
c. printf (“ %d”, * (TTT(a) ));
d. printf (“ %d”, * (TTT(struct dato a) ));
e. printf (“ %d”, (TTT(a ) +i));

