//Cuál es la expresión correcta para la sentencia de código que se encuentra
//asociada al while?

typedef struct {
 int a;
}ff_t;

//...
int main ( ) {
  int a;
  struct ff_t ff;
// declaraciones correctas
//...
while ( calcula(a) == ff.a ) {
}

Seleccione una:
a. El while compara elementos definidos mediante un struct.
b. calcula y ff son colecciones de elementos de distinto tipo.
c. La letra  “a” de los  operandos representa la misma variable “a”.
d. La expresión del ciclo while no es correcta.
e. La letra  “a” de los operandos  no representa la misma variable “a”.
