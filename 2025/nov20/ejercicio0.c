#include <stdio.h>
#define TAM 10000

int main(void) {

  int *arreglo;

  // 1. Reservar espacio para TAM elementos
  // 2. Inicializar el arreglo dinámico con números
  // aleatorios entre -100 y 10000
  // 3. Ordenar el arreglo
  //    void ordenar(int *arreglo, int tam);
  // 4. Imprimir el arreglo
  //    void imprimir(int *arreglo, int tam);
  // 5. Solicitar al usuario un número num
  // 6. Buscar el elemento en el arreglo ordenado
  //    int buscar_num_mbinario(int *arreglo, int tam, int num);
  //    devuelve el índice donde se encuentra el elemento
  //    -1 si no existe
  // 7. Imprimir la posición del elemento buscado si existe


  return 0;
}
